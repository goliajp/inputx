[package]
name = "inputx-wubi-cement"
version = "2.0.1"
edition.workspace = true
rust-version.workspace = true
license = "MIT OR Apache-2.0"
readme = "README.md"
description = "[DEPRECATED v2.0 → see inputx-wubi-data in v1.6 backlog] Wubi data + IDF reader + layer helpers. The stateful WubiEngine state machine was carved out of this crate in v1.5.2 — for engine code, copy from the Inputx monorepo's inputx-core/src/wubi/engine.rs (the cement-as-application-source taxonomy correction)."
authors = ["GOLIA K.K."]
repository = "https://github.com/goliajp/inputx"
homepage = "https://github.com/goliajp/inputx"
keywords = ["ime", "wubi", "engine", "cement"]
categories = ["text-processing"]

[lib]
crate-type = ["rlib"]

[dependencies]
# v1.6 thin re-export shim: this crate now `pub use inputx_wubi_data::*;`
# only. Historical (1.4.x / 2.0.0) consumers continue to compile
# transparently; new code should depend on inputx-wubi-data directly.
inputx-wubi-data = { path = "../inputx-wubi-data", version = "1.6" }
