# weights.tsv and provenance.toml ARE committed — that's the whole point of
# the pipeline (reproducible, version-controlled weights). This file just
# keeps the directory present in fresh clones before either has been built.
