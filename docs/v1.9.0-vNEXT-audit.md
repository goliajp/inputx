# v1.9.0 WU-π — vNEXT Audit

**Status**: ✅ **CLOSED 2026-06-06** — see §"Close-out (2026-06-06)" at the bottom.

Original audit: 2026-06-01. Cherry-pick batch + close-out: 2026-06-06.

## What ran

```sh
./tools/dict-pipeline/run.sh
```

Harvest source: `zh-wikipedia` dump 20260501, 68,786 articles parsed,
200,000 unique words written (top by raw count).

Diff outputs:
- `tools/dict-pipeline/output/zh-wikipedia-vs-pinyin/{new,dropped,freq_changes}.tsv`
- `tools/dict-pipeline/output/zh-wikipedia-vs-wubi/{new,dropped,freq_changes}.tsv`

vNEXT idf snapshot: **byte-identical to shipped** (per `run.sh` design
— "today: vNEXT snapshot is byte-identical to shipped because input
data unchanged"). Pipeline currently audits diff but doesn't actually
merge wiki corpus into dict generation.

## Initial diff (pre-TC-filter)

| Engine | new_entries | dropped | freq_changes |
|---|---:|---:|---:|
| pinyin | 108,259 | 275,770 | 89,655 |
| wubi   | 155,804 |  87,394 | 42,192 |

Top "new entries" was 90% **traditional Chinese characters**
(於 / 為 / 與 / 中國 / 美國 / 一個 / 開始 / 大學 ...) — wiki has heavy
zh-tw editor contributions; the dict only stores simplified, so
TC↔SC pairs show as missing on the SC side.

## After TC filter (v1.9.0 WU-π.a change)

Added `tools/dict-pipeline/diff_corpus.py::has_tc_char` filter that
skips any harvest row containing a character in
`core/crates/inputx-core/data/tc_chars_demote.txt` (the 3549-char
OpenCC t2s-derived TC marker set). TC filter ON drops 70,069 rows.

| Engine | new_entries | dropped | freq_changes |
|---|---:|---:|---:|
| pinyin | 38,221 *(-65%)* | 275,801 | 89,606 |
| wubi   | 87,791 *(-44%)* |  89,450 | 40,018 |

**Top "new entries" content after filter**:
- **音译外语片段**: 大利 / 斯特 / 利亚 / 克斯 / 尼亚 / 拉斯 / 里斯 / 德里 (国名/人名切片)
- **wiki 行文专用**: 参考文献 / 这一 / 日至 / 日起 / 号线 / 集第 / 之名 / 之意
- **台湾地理/政治**: 新北市 / 苗栗 / 中市 / 平成 / 水扁
- **真实 dict gap**: 不是 / 二次 / 第一次 / 第二次 / 三年 / 四年 / 一部分 / 一名 (这些 wubi dict 应该有，确实缺)

**Top "dropped" content** (in dict, not in wiki — informational only):
- 大量**口语/聊天表达**: 哦哦哦 / 谁谁谁 / 长见识了 / 还行 / 闲着没事 / 偷着乐 / 待会儿 / 用得着
- 反映 wiki 文风正式 vs IME 用户口语化的 corpus mismatch

## Findings

1. **Wiki not suitable as primary IME corpus** — freq distribution
   skews toward proper nouns + historical/political content, missing
   the casual conversational vocabulary that drives real IME usage.
2. **Pipeline missing the merge step** — `tools/dict-pipeline/run.sh`
   only emits diff reports; the vNEXT `.idf` rebuild step uses the
   unchanged shipped weights.tsv, so wiki freq never actually flows
   into the dict. To ship a new dict you'd need a `pinyin-build-
   weights` invocation with wiki added as a corpus source (which is
   a 1-2 day refactor of `core/crates/inputx-pinyin/tools/build_
   weights.rs`).
3. **Audit value is in gap-identification, not bulk replacement** —
   the diff is most useful for spot-checking specific missing
   entries (wubi dict has no `不是` / `二次` / `第一次`!) than for a
   wholesale corpus blend.

## Trigger evaluation

PLAN.md L4 v1.9.0 → v1.9.1 trigger:
- (a) `data/private-dict/vNEXT/*` 落盘 — **✓** (byte-identical to
  shipped, but exists)
- (b) 3 个 diff report TSV 生成 + 内容看起来"自然语言进化" —
  **✗ NOT MET**. After TC filter the diff is cleaner but still
  shows wiki-specific noise (segmentation fragments, proper nouns)
  dominating the top. Wholesale promotion would degrade IME quality.
- (c) `docs/v1.9.0-vNEXT-audit.md` 写明 audit decisions — **✓** (this file)

## v1.9.0 WU-π.b update — corpus pivot to OpenSubtitles

Following the original audit, user picked option **B: replace wiki
with an IME-fit corpus**. Implementation lands as a second source in
`manifest.yaml` (wiki kept for cross-source comparison, OpenSubtitles
now the primary signal):

```
| source           | license   | size  | IME-fit |
| ---------------- | --------- | ----- | ------- |
| zh-wikipedia     | CC-BY-SA  | ~3GB  | low     |
| zh-opensubtitles | CC-BY 4.0 | 172MB | high    |
```

### Subtitle diff (with TC filter ON)

OPUS v2018 zh_cn dump, all 19M+ lines parsed.

| Engine | new_entries | dropped | freq_changes |
|---|---:|---:|---:|
| pinyin | 64,313 *(vs wiki 38,221)* | 261,965 | 102,186 |
| wubi   | 120,109 *(vs wiki 87,791)* | 81,840 | 47,470 |

**Top "new entries" content** (after TC filter):

*OpenSubtitles vs pinyin* (top 30):
> 这是 · 我要 · 我能 · 我来 · 有个 · 他会 · 雅黑 · 不起 · 将会 ·
> 找个 · 只会 · 我刚 · 你别 · 干得 · 我该 · 那好 · 很难 · 宋体 ·
> 不去 · 见过 · 人会 · 这才 · 这会 · 还会 · 我想 · 做些 · 快去 ·
> 他来 · 全是 · 不像

*OpenSubtitles vs wubi* (top 30):
> 不是 *(freq 301k!)* · 不要 · 我要 · 我会 · 有人 · 干嘛 · 我能 ·
> 看起 · 看起来 · 没什么 · 我来 · 打电话 · 怎么回事 · 克斯 · 没关 ·
> 太好了 · 他会 · 可不 · 是因为 · 大人 · 第一次 · 像是 · 活着 ·
> 开玩笑 · 会儿 · 任何人 · 好意思 · 斯特 · 留在 · 公室

These are **real IME-relevant gaps** — not segmentation noise or
proper nouns. Wubi dict missing `不是 / 不要 / 干嘛 / 太好了 / 第一次
/ 任何人 / 好意思 / 开玩笑 / 打电话 / 怎么回事 / 是因为 / 大人 /
活着 / 留在` (freq 10k–300k each) is genuine polish opportunity.

Residual noise: jieba segmentation artifacts (`会儿` / `公室` / `没关`
= truncations of 一会儿 / 办公室 / 没关系) ~5%.

### Trigger evaluation (re-evaluation post-pivot)

PLAN.md L4 v1.9.0 → v1.9.1 trigger:
- (a) `data/private-dict/vNEXT/*` 落盘 — **✓** (byte-identical
  still; pipeline still lacks merge step)
- (b) 3 个 diff report TSV 生成 + 内容看起来"自然语言进化" —
  **✓ NOW MET** for the zh-opensubtitles-vs-* pair. Top entries are
  real IME-relevant gaps, ≤ 5% jieba-artifact noise.
- (c) audit log written ✓

Outstanding scope item: **pipeline still lacks the corpus-merge
step** — vNEXT byte-identical to shipped means nothing's actually
ship-able yet. WU-π.c (or v1.9.1 WU-ρ) needs to either:
  (i) extend `build_weights.rs` to accept harvest TSV as a weighted
      input corpus (proper corpus merge), or
  (ii) manually cherry-pick the top ~50 real-gap entries into a
       `BAKED_ADDITIONS` table in the idf builders (matches the v1.4
       pinyin polish-log pattern).

Option (ii) is the faster ship; option (i) is the principled long-
term answer. The audit favors landing (ii) FIRST as a quick polish
ship, then doing (i) for v1.9.2+.

## v1.9.0 WU-π.d update — corpus merge reverted, cherry-pick backlog

After WU-π.c shipped the full corpus merge (commit b25d611) the
baseline regression was 10/288 fails (6/24 in baseline). WU-π.c.1
(commit dfc46c5) partially fixed it — 8/288 fails, 4/24 in baseline.
The remaining 4 baseline fails clustered around independent
calibration problems (Auto-layer rare demote, TC line filter in
build_weights, Viterbi long-phrase suppression, prior_correction
re-tune) each needing its own small fix.

Per user 2026-06-01 ("Revert OPUS 整合，走 cherry-pick (option ii)"),
both b25d611 (corpus merge) and dfc46c5 (partial fix) were reverted:
- 587a27b: revert of dfc46c5
- dfc679d: revert of b25d611

Post-revert: `cargo test -p inputx-core --lib baseline` 24/24 ✓,
`cargo test -p inputx-core --lib` 288/0 ✓. Workspace state matches
v1.8.2 ship + the WU-π.a/π.b audit-side tooling (TC filter,
OpenSubtitles harvester, dict-pipeline multi-source iteration).

### Cherry-pick backlog (deferred to v1.9.1+)

From the WU-π.b OpenSubtitles diff, the real IME-relevant gaps that
*could* be manually added to the shipped dicts under the option (ii)
"polish-log pattern" are:

**Wubi phrase gaps** (require correct wubi-86 phrase encoding per
`tools/wubi-import-phrases.rs` rules — first 2 codes of each char
for ≤2-char, first code of each char for 3-char, etc.):
- `不是` (freq 301k in OPUS), `不要` (123k), `我要` (101k), `我会` (83k)
- `有人` (60k), `干嘛` (31k), `我能` (28k), `没什么` (23k), `我来` (23k)
- `打电话` (21k), `怎么回事` (20k), `太好了` (16k), `第一次` (13k)
- `第二次`, `任何人` (12k), `好意思` (12k), `开玩笑` (13k), `留在` (11k)

**Pinyin BAKED_ADDITIONS gaps**: top "new" entries from the OPUS diff
are mostly *bigram fragments* (`这是` / `我要` / `我能` / `有个`) that
jieba doesn't tokenize as compound words and the pinyin dict
deliberately omits to leave room for natural Viterbi composition.
These should NOT be cherry-picked into `BAKED_ADDITIONS` — they'd
pollute the dict the way `片你` did pre-v1.6. The handful of real
single-token gaps from the diff need a separate manual audit.

**Recommended workflow** (for next session):
1. Filter the OPUS-vs-wubi `new_entries.tsv` to phrases the wubi
   facade's `phrase-encode` rules can encode → that's the cherry-pick
   candidate pool.
2. For each candidate: compute the wubi-86 code via the existing
   `tools/wubi-import-phrases.rs` encoder, verify the candidate isn't
   already present at a different code, add to
   `core/crates/inputx-wubi/data/phrases.txt`.
3. Re-run `wubi-build-weights` + `idf-from-wubi-tables` + baseline
   tests.
4. Don't cherry-pick to pinyin (pinyin gaps are mostly Viterbi-
   composition territory).

Time estimate: ~5 minutes per wubi candidate × 20 candidates ≈ 2 hours
manual work. Better suited to a dedicated session.

## Recommendations (for next session)

Pick one of:

**A. Targeted manual fix**: cherry-pick the ~20 real `dict gap`
entries from wubi new_entries (`不是` / `二次` / `第一次` / `第二次` /
`一部分` / `一名` / `有人` / `三年` / `四年` / `州市` / `北市` /
`京市` / `部份` / `三世` ...) into `core/crates/inputx-wubi/data/
weights/weights.tsv` manually. Skip the wiki-pollution categories.
Re-run `pinyin-build-weights` / `wubi-build-weights`. ~half-day.

**B. Drop wiki as primary corpus, pick a better source**: Common Crawl
zh subset, OSCAR zh-CN, or a chat-style corpus (license permitting —
CC0/Apache/MIT only per D19). Re-run pipeline with the new source,
expect a different (hopefully more IME-relevant) diff. ~1-2 days
research + harvest implementation.

**C. Implement actual corpus merge** with weighted blending: extend
`build_weights.rs` to accept a `harvest TSV + blend_alpha` arg, mix
wiki freq into the existing weights at a configurable ratio.
~2-3 days. High risk — wiki noise gets quietly amplified unless the
blend is heavily down-weighted (blend_alpha < 0.1).

**D. Abandon v1.9 corpus-driven track**, pivot v1.9 to **telemetry-
driven calibration** (the v1.10 plan) — wire polish-log to
EngineWeights knob auto-adjustment. Ship dict updates as one-off
manual fixes (option A pattern) outside the cycle structure.

User decision required before WU-π.b (evaluate clean diff) advances.

---

## Close-out (2026-06-06) — cherry-pick batch shipped

Per the original audit's "Recommendations" section (option **A** —
targeted manual fix) and user 2026-06-01 directive "Revert OPUS 整合，
走 cherry-pick (option ii)", landed the cherry-pick batch on
`polish/v1.9-wu-pi-cherry-pick` branch.

### Phrases added (22 total) to `core/crates/inputx-wubi/data/library.tsv`

All encoded per wubi-86 standard (王永民 1986):
  - 2-char: char1.zigen[0..2] + char2.zigen[0..2]
  - 3-char: char1.zigen[0] + char2.zigen[0] + char3.zigen[0..2]
  - 4-char: each char's first zigen × 4
  - ≥5-char: char1[0] + char2[0] + char3[0] + last[0]

Each char's canonical zigen letters looked up from existing library
entries (Auto-layer 4-letter codes), then sliced per the rule above.

| code | phrase   | freq    | mixed-mode rank after add |
|------|----------|--------:|---------------------------|
| gijg | 不是     | 301000  | #0 ✓ (over 还是 40k)      |
| gisv | 不要     | 123000  | #0 ✓                      |
| trsv | 我要     | 101000  | #0 (only)                 |
| trwf | 我会     |  83000  | #0 (only)                 |
| deww | 有人     |  60000  | #0 ✓                      |
| fgky | 干嘛     |  31000  | #0 ✓                      |
| trce | 我能     |  28000  | #0 ✓                      |
| iwtc | 没什么   |  23000  | #0 ✓                      |
| trgo | 我来     |  23000  | #0 ✓                      |
| rjyt | 打电话   |  21000  | #0 ✓                      |
| dvbn | 太好了   |  16000  | #0 ✓                      |
| vuln | 好意思   |  12000  | #0 (only)                 |
| jlyl | 是因为   |  20000  | #0 (only)                 |
| itud | 活着     |  30000  | #0 ✓                      |
| ttlg | 怎么回事 |  20000  | #1 (自行车 27786 wins)    |
| tguq | 第一次   |  13000  | #1 (生效 30230 wins)      |
| tfuq | 第二次   |  12000  | #2 (行将/等效 win)        |
| ggtt | 开玩笑   |  13000  | #2 (来来往往/五笔 win)    |
| qydh | 留在     |  11000  | #1 (留存 21189 wins)      |
| rfgo | 看起来   |  23000  | #1 (反过来 23496 wins)    |
| ddww | 大人     |  30000  | #1 (大众 30093 wins)      |
| wwww | 任何人   |  12000  | #4 (人 L2 structural win) |

### Non-#0 placements analysis

8 of 22 phrases land at #1-#4 due to existing high-freq peers at the
same wubi code. All remain VISIBLE in the user's top-5 panel — they're
"reachable via number key" rather than "auto-committed". This matches
canonical wubi-86 UX where multi-char phrases routinely share codes
and the user picks among them.

`任何人 wwww` is the only structurally-pinned case: `人` is a Jianma2
single-char (layer 2 > Phrase layer 1), so 任何人 cannot reach #0
regardless of freq. User intending 任何人 either: (a) presses number
key 5 to select it from the visible top-5, or (b) types via pinyin
`renheren`. Acceptable.

If a user reports any specific phrase wanting #0 (Class B polish),
boost via `tools/scoring/data/polish/quickfix_boost.tsv` per the
polish skill protocol.

### Pipeline state

Per the original audit, the dict-pipeline still lacks the actual
corpus-merge step — `data/private-dict/vNEXT/*` remains byte-identical
to shipped at the pipeline-output level. The cherry-pick lands
DIRECTLY in `core/crates/inputx-wubi/data/library.tsv` (`source=polish`)
— same channel as reactive polishes — bypassing the pipeline.

The `tools/dict-pipeline/run.sh` audit infrastructure (multi-source
harvest, TC filter, OPUS integration) stays in place for future
cycles where a real corpus-merge re-runs against newer harvest dates.

### v1.9.0 → v1.9.1 trigger (re-evaluation)

PLAN.md L4 v1.9.0 → v1.9.1 trigger:
- (a) `data/private-dict/vNEXT/*` 落盘 — ✓ (byte-identical to shipped;
  the meaningful "new content" lives in library.tsv per option-ii path)
- (b) 3 个 diff report TSV 生成 + 内容自然 — ✓ (OPUS-vs-* pair, original
  audit signed)
- (c) `docs/v1.9.0-vNEXT-audit.md` 写明 audit decisions — ✓ (this file)
- (NEW) cherry-pick from audit's actionable backlog — ✓ (22 phrases
  landed)

**WU-π CLOSED.** Next: WU-ρ (baseline-fixture diff vs vNEXT).

### Test + deploy results

    make polish-rebuild → baseline 46/46 ✓, lib 312/0 ✓
    mac/reinstall.py   → probe returned 你好, clean LS singleton

### Commit

  polish(A:wubi): WU-π cherry-pick 22 OPUS-audited phrase gaps
  branch: polish/v1.9-wu-pi-cherry-pick
