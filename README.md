# Inputx

A privacy-first Chinese input method (IME) for iOS, with a dual-engine core
written in Rust.

Inputx types Chinese the way 万能五笔 / 搜狗五笔 do — **Wubi 86 as the primary
engine, Pinyin as an automatic fallback** — so you never manually switch input
schemes. It runs entirely on-device: no network, no analytics, no "Allow Full
Access" required.

## Highlights

- **Dual engine, no manual switch** — Wubi 86 candidates first, Pinyin
  (full + 简拼 initials) fills in when Wubi yields nothing.
- **On-device learning (L0)** — three picks of the same `(input → word)` pair
  auto-promotes it to the top. Learned data never leaves the device and is
  user-exportable/importable as JSON.
- **Privacy by construction** — zero network calls, zero telemetry, no full
  keyboard access. The only data collected is whatever Apple's built-in crash
  reporter captures.
- **Rare CJK support** — optional display of Plane-2+ characters via a bundled
  cascade font (`Lab8CJKExtended`, a Plangothic subset, OFL).
- **CJK typography** — full-width / half-width toggle, ASCII→CJK punctuation
  mapping, stateful smart quotes.
- **iOS-native UX** — Dynamic Type, dark mode, VoiceOver, landscape reflow,
  per-key long-press variants, source indicators on candidates.

## Architecture

```
┌─ ios/  ── Swift keyboard extension + container app (the ship target)
│            calls into the Rust core over a C FFI
│
├─ core/ ── inputx-core  (Rust, cdylib + rlib + C header via cbindgen)
│            composite/   dual-engine router (Wubi primary, Pinyin fallback)
│            locale/      CJK punctuation + full/half width
│            ffi.rs       C ABI consumed by Swift
│
└─ mac/  ── macOS shell — FROZEN until iOS v1 ships (see ROADMAP)
```

The two language engines are separately published, MIT/Apache-2.0
open-source libraries maintained alongside this repo:

| Library | crates.io | npm | Repo |
|---|---|---|---|
| Wubi 86 | [`wubi`](https://crates.io/crates/wubi) | [`@goliapkg/wubi`](https://www.npmjs.com/package/@goliapkg/wubi) | [goliajp/wubi](https://github.com/goliajp/wubi) |
| Pinyin | [`golia-pinyin`](https://crates.io/crates/golia-pinyin) | [`@goliapkg/pinyin`](https://www.npmjs.com/package/@goliapkg/pinyin) | [goliajp/pinyin](https://github.com/goliajp/pinyin) |

`inputx-core` is the thin composite layer on top of them: dual-engine
dispatch/merge, L0 user-learning across both engines, locale handling, and the
C FFI the iOS keyboard talks to.

## Build

Requires Rust ≥ 1.85 (edition 2024), Xcode 16+, and
[`xcodegen`](https://github.com/yonaskolb/XcodeGen).

```sh
# Rust core — runs the full test suite
cd core && cargo test --release

# iOS — builds the Rust static lib, regenerates the Xcode project
# (ios/Inputx.xcodeproj is .gitignored; project.yml is the source of truth),
# and builds + installs onto the simulator.
cd ios && ./build_sim.sh

# On-device build (requires a provisioning profile / signing identity)
cd ios && ./build_device.sh
```

The C header (`core/include/inputx_core.h`) is generated by cbindgen during
`cargo build` and is `.gitignore`d, so a fresh clone must build the Rust core
before the iOS target can link. The build scripts handle this ordering for you.

## Status

iOS v1, targeting the App Store. The Rust engine, dual-engine routing, L0
persistence, locale handling, and the iOS keyboard/settings UI are all
implemented and tested. Remaining work is on-device verification, performance
profiling, TestFlight, and App Store submission — see [`ROADMAP.md`](ROADMAP.md)
for the linear checklist.

Out of scope for v1: the macOS shell (frozen), iCloud L0 sync, and ja-JP /
ko-KR localization.

## License

[MIT](LICENSE) © 2026 GOLIA K.K.

Bundled third-party data and resources retain their own licenses — see the
in-app Acknowledgements screen and the sibling library repos for the full
attribution chain (Unihan, jieba, Leipzig, SUBTLEX-CH, Plangothic / OFL).
